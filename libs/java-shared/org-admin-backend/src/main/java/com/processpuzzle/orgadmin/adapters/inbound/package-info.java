/**
 * Inbound adapters: the two REST controllers, their mapper, and the advice that turns this feature's
 * refusals into {@code ErrorResponse} bodies.
 *
 * <p>Not a named interface. Nothing outside this module calls a controller.
 */
package com.processpuzzle.orgadmin.adapters.inbound;
