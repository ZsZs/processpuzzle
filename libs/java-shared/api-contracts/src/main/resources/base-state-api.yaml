openapi: 3.0.3
info:
  title: ProcessPuzzle State Machine API (base-state)
  description: >-
    Two layers, mirroring base-entity and base-rule's split.

    KNOWLEDGE LAYER (/state-machines): the state machine *definition* for one entity type —
    its states, transitions, triggers, guards and actions. Exactly one state machine per
    entityName (1:1, enforced at create time), so the definition is addressed by entityName
    itself rather than an independent key — same convention as base-entity-api.yaml's
    /entity-definitions/{entityName}. No parallel states and no nested states in this
    version; every State is a flat, mutually exclusive value.

    OPERATION LAYER (/entities/{entityName}/{objectId}/state, .../state-transitions): the
    current state of one EntityObject, and the single entry point through which it changes.
    There is no separate "state" storage here — current state is an ordinary attribute on
    the EntityObject's JSONB payload (see base-entity-api.yaml EntityObject), named by this
    state machine's stateAttributeKey. Storing it that way means RSQL filtering, sorting and
    search on state (`state==Approved`) work through base-entity's existing
    AttributePathResolver / JsonPathExpressionBuilder pipeline for free — base-state does not
    reimplement querying.

    DIAGRAM LAYER (/diagrams/{entityName}): where the states and transitions of one machine sit
    on the graphical modeler's canvas. Purely presentational and a resource of its own rather
    than fields on StateMachineDefinition, so that dragging a node never risks a whole-document
    replace of the topology — the two are written by different gestures at very different rates.
    1:1 with the state machine it lays out, and addressed by the same entityName.

    ## Ownership and the single entry point

    base-state-backend is the only writer of the state attribute. base-entity-backend stays
    unaware of state machines entirely; it does not gain an AOP aspect or a special case for
    this attribute in this version, unlike base-rule's `@Validate` aspect which genuinely
    sits in the write path because it validates arbitrary attribute changes. State has
    exactly one legitimate entry point instead: fireStateTransition below. The one write that
    is not a transition is the *first* one — base-state observes base-entity's ObjectCreated
    event and writes initialStateKey onto the new object, which is why a caller never has to
    supply a state on create and why base-entity needs no knowledge of machines to start one.
    base-state-backend
    calls into base-entity-backend's operation-layer update as a privileged internal caller
    once a transition's guards pass, and publishes EntityObjectStateChangedEvent
    post-commit (via Spring Modulith) so base-workflow-backend can react to state changes it
    did not itself trigger — e.g. a user pressing "Approve" in the UI. base-workflow-backend
    is otherwise an ordinary caller of fireStateTransition, with no special-cased path of its
    own; see base-workflow-api.yaml's note that state transitions are delegated to base-state
    by id only.

    ## Triggers, not target states

    TransitionRequest names a `triggerKey` (the verb a caller invokes — "approve", "reject")
    rather than a `targetStateKey`. The engine resolves `(currentStateKey, triggerKey) ->
    Transition` itself, so callers never need to know the machine's topology: a UI button is
    wired to a trigger, not to a specific edge. This also allows two different triggers to
    reach the same target state under different guards/actions, and one trigger to mean
    different things depending on the source state.

    ## Business rejection vs. optimistic-lock conflict

    Firing a transition that is simply not available from the current state (wrong trigger,
    or a guard rejects it) is an expected, common outcome — not an exceptional one — so it is
    answered `200` with `TransitionResult.success = false` and a `rejectionReason`, the same
    way a disabled UI button is a normal state rather than an error. `409` is reserved for a
    genuine optimistic-lock conflict: `TransitionRequest.version` no longer matches the
    EntityObject's current version. `400` is reserved for structurally invalid requests (an
    unknown `triggerKey` that exists on no transition at all, malformed context).

    ## Multi-tenancy

    Scoped by {orgKey} exactly as base-entity-api.yaml and base-rule-api.yaml establish; see
    shared-api.yaml OrgKeyParam. entityName resolves within the same organization only.
  version: 0.1.0

servers:
  - url: 'http://localhost:8080/api/v1'

paths:

  # ── Knowledge layer: state machine definitions ─────────────────

  /organizations/{orgKey}/state-machines:
    parameters:
      - $ref: "#/components/parameters/OrgKeyParam"
    get:
      operationId: listStateMachineDefinitions
      summary: List the organization's state machine definitions
      tags: [ Base State ]
      parameters:
        - $ref: "#/components/parameters/WhereParam"
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/SizeParam"
      responses:
        "200":
          description: Paged list of state machine definitions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageOf_StateMachineDefinition"
        "403":
          $ref: "#/components/responses/Forbidden"

    post:
      operationId: createStateMachineDefinition
      summary: Create a state machine definition for an entity type
      description: >-
        Rejected with 409 if entityName already has a state machine definition in this
        organization — the 1:1 relationship is enforced here, not left to the caller.
        Validated at creation: initialStateKey and every Transition's sourceStateKey /
        targetStateKey must resolve to a declared State.key; no two transitions may share
        the same (sourceStateKey, triggerKey) pair, since that would make trigger resolution
        ambiguous; stateAttributeKey must name an attribute declared on the entityName's
        current EntityDefinition.
      tags: [ Base State ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StateMachineDefinitionInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StateMachineDefinition"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: entityName does not resolve to an entity definition in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: entityName already has a state machine definition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /organizations/{orgKey}/state-machines/{entityName}:
    parameters:
      - $ref: "#/components/parameters/OrgKeyParam"
      - $ref: "#/components/parameters/EntityNameParam"
    get:
      operationId: getStateMachineDefinition
      summary: Get the state machine definition for an entity type
      tags: [ Base State ]
      responses:
        "200":
          description: The state machine definition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StateMachineDefinition"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      operationId: updateStateMachineDefinition
      summary: Replace a state machine definition's states and transitions
      description: >-
        Whole-document replace. Optimistic-locked the same way base-document's
        updateDocument is: the server loads the managed entity and lets Hibernate's own
        `@Version` column detect a genuinely concurrent write on flush — there is no
        separate client-supplied version to compare, unlike TransitionRequest.version below,
        which guards a read-then-act sequence rather than a single write. Removing a State
        or Transition that an in-flight EntityObject currently depends on is accepted — the
        definition is the source of truth going forward — but any object left sitting in a
        currentStateKey no longer declared surfaces that fact the next time
        getEntityObjectState is called (isFinal true, availableTransitions empty), rather
        than being migrated automatically.
      tags: [ Base State ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StateMachineDefinitionInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StateMachineDefinition"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

    delete:
      operationId: deleteStateMachineDefinition
      summary: Delete a state machine definition
      description: >-
        Existing EntityObjects keep whatever value is already stored in their state
        attribute; base-state simply stops governing further changes to it, and
        fireStateTransition/getEntityObjectState both answer 404 for this entityName from
        then on.
      tags: [ Base State ]
      responses:
        "204":
          description: Deleted
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /organizations/{orgKey}/state-machines/import:
    parameters:
      - $ref: "#/components/parameters/OrgKeyParam"
    post:
      operationId: importStateMachineDefinitions
      summary: Bulk-import state machine definitions from a YAML file
      description: >-
        All-or-nothing, same contract as base-rule's importRules: if any entry fails
        validation (duplicate entityName, unknown state/transition reference, ambiguous
        trigger), nothing in the file is persisted and the full list of problems is
        returned. Imported definitions are scoped to {orgKey} regardless of any
        organization key present in the file.
      tags: [ Base State ]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [ file ]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Import outcome
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResult"
        "403":
          $ref: "#/components/responses/Forbidden"

  /organizations/{orgKey}/state-machines/export:
    parameters:
      - $ref: "#/components/parameters/OrgKeyParam"
    get:
      operationId: exportStateMachineDefinitions
      summary: Export state machine definitions as a YAML file
      tags: [ Base State ]
      parameters:
        - name: entityName
          in: query
          required: false
          description: Limit the export to a single entity type's state machine.
          schema:
            type: string
      responses:
        "200":
          description: YAML file
          content:
            application/x-yaml:
              schema:
                type: string
                format: binary
        "403":
          $ref: "#/components/responses/Forbidden"

  # ── Diagram layer: the graphical modeler's layout ───────────────

  /organizations/{orgKey}/diagrams:
    parameters:
      - $ref: "#/components/parameters/OrgKeyParam"
    get:
      operationId: listDiagramDefinitions
      summary: List the organization's diagram layouts
      tags: [ Base State Diagrams ]
      parameters:
        - $ref: "#/components/parameters/WhereParam"
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/SizeParam"
      responses:
        "200":
          description: Paged list of diagram layouts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageOf_DiagramDefinition"
        "403":
          $ref: "#/components/responses/Forbidden"

  /organizations/{orgKey}/diagrams/{entityName}:
    parameters:
      - $ref: "#/components/parameters/OrgKeyParam"
      - $ref: "#/components/parameters/EntityNameParam"
    get:
      operationId: getDiagramDefinition
      summary: Get the diagram layout of an entity type's state machine
      description: >-
        404 when the machine has never been arranged. That is not an error condition for the
        modeler — it is how it learns to fall back to an automatic layout — and it is
        deliberately distinguishable from a layout that exists but is empty.
      tags: [ Base State Diagrams ]
      responses:
        "200":
          description: The diagram layout
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiagramDefinition"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      operationId: saveDiagramDefinition
      summary: Create or replace the diagram layout of an entity type's state machine
      description: >-
        Whole-document replace, and an **upsert** — `201` the first time an entityName is
        arranged, `200` every time after. Deliberately not the POST-create / PUT-replace pair
        the /state-machines endpoints above use: there, a second definition for the same
        entityName is a genuine user error worth a 409, whereas saving a layout twice is the
        normal case. The client always knows entityName, so no key is ever server-assigned and
        an upsert costs it neither a GET-then-decide round trip nor a race on first save.

        Requires a state machine definition to exist for entityName — 404 otherwise, since a
        layout for a machine that does not exist has nothing to lay out. Individual `stateKey`
        and `transitionKey` values are **not** validated against it: updateStateMachineDefinition
        is free to drop a state, a layout entry naming a key the machine no longer declares is
        harmless (the modeler ignores what it cannot render, and the next save prunes it), and
        rejecting it would make two independent, individually-valid saves fail depending only
        on the order they arrive in.

        Optimistic-locked the same way updateStateMachineDefinition is: Hibernate's own
        `@Version` column detects a genuinely concurrent write on flush.
      tags: [ Base State Diagrams ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiagramDefinitionInput"
      responses:
        "200":
          description: Replaced
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiagramDefinition"
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiagramDefinition"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: entityName has no state machine definition in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          $ref: "#/components/responses/Conflict"

    delete:
      operationId: deleteDiagramDefinition
      summary: Discard the diagram layout of an entity type's state machine
      description: >-
        Resets the machine to an automatic layout; the state machine definition itself is
        untouched. Deleting the state machine definition discards its layout too, so this
        endpoint exists for the "re-arrange from scratch" gesture rather than for cleanup.
      tags: [ Base State Diagrams ]
      responses:
        "204":
          description: Deleted
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ── Operation layer: an object's current state ──────────────────

  /organizations/{orgKey}/entities/{entityName}/{objectId}/state:
    parameters:
      - $ref: "#/components/parameters/OrgKeyParam"
      - $ref: "#/components/parameters/EntityNameParam"
      - $ref: "#/components/parameters/ObjectIdParam"
    get:
      operationId: getEntityObjectState
      summary: Get an object's current state and the transitions available from it
      description: >-
        availableTransitions is a dry run: every Transition declared with sourceStateKey
        equal to the object's currentStateKey, each with its guards evaluated against the
        object's *current* payload but no actions executed — intended for a UI to render
        "Approve" / "Reject" buttons as enabled or disabled without firing anything.
        guardsSatisfied can go stale the moment another change lands; fireStateTransition
        re-evaluates guards itself and is the only call that is authoritative.
      tags: [ Base State ]
      responses:
        "200":
          description: Current state and available transitions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntityObjectStateView"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: >-
            Either the object does not exist, or entityName has no state machine definition
            in this organization.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /organizations/{orgKey}/entities/{entityName}/{objectId}/state-transitions:
    parameters:
      - $ref: "#/components/parameters/OrgKeyParam"
      - $ref: "#/components/parameters/EntityNameParam"
      - $ref: "#/components/parameters/ObjectIdParam"
    post:
      operationId: fireStateTransition
      summary: Attempt to fire a transition by trigger
      description: >-
        Resolves (currentStateKey, triggerKey) to a Transition, evaluates its guards in
        declaration order against the object's current payload (AND semantics,
        short-circuiting on the first guard that rejects), and — only if every guard
        passes — runs its actions in declaration order, writes the new currentStateKey to
        the entity object's state attribute via base-entity-backend, and publishes
        EntityObjectStateChangedEvent post-commit. See the top-level description for the
        200-vs-409 distinction between a business rejection and a stale-version conflict.
      tags: [ Base State ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransitionRequest"
      responses:
        "200":
          description: >-
            Always returned once the request itself is well-formed and the version is
            current — check TransitionResult.success for the outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TransitionResult"
        "400":
          description: triggerKey matches no transition declared anywhere on this state machine, or context is malformed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: >-
            Either the object does not exist, or entityName has no state machine definition
            in this organization.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: TransitionRequest.version no longer matches the object's current version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  # ── Translations ──
  # The UI message bundles this feature owns, in the shape the frontend's asset files have. Two
  # templates rather than one with an optional segment: 4 vs 5 segments is what makes them
  # unambiguous, and it mirrors `assets/i18n/<lang>.json` and `assets/i18n/<scope>/<lang>.json`.
  #
  # `state` is the feature's own service root. Every feature serves its own translations from its
  # own store, so that a later split into services with separate databases moves this segment into the
  # host rather than rewriting the resource. In the monolith it is also what keeps seven identical
  # `/translations` paths from colliding into an ambiguous mapping at startup.
  /organizations/{orgKey}/state/translations/{locale}:
    parameters:
      - $ref: '#/components/parameters/OrgKeyParam'
      - $ref: '#/components/parameters/TranslationLocaleParam'
    get:
      tags:
        - Base State Translations
      summary: Root translation bundle of this feature for one language
      operationId: getStateTranslations
      description: >-
        The bundle that has no transloco scope — the equivalent of `assets/i18n/<lang>.json`.
        Answers 200 with an empty object when nothing has been seeded, rather than 404: the
        frontend loader reaches here only after an asset miss, and an error status would make an
        unseeded locale indistinguishable from a broken backend.
      responses:
        '200':
          description: The bundle, empty when nothing has been seeded for this locale.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TranslationBundle'
        '403':
          $ref: '#/components/responses/Forbidden'
  /organizations/{orgKey}/state/translations/{scope}/{locale}:
    parameters:
      - $ref: '#/components/parameters/OrgKeyParam'
      - $ref: '#/components/parameters/TranslationScopeParam'
      - $ref: '#/components/parameters/TranslationLocaleParam'
    get:
      tags:
        - Base State Translations
      summary: One scoped translation bundle of this feature
      operationId: getStateScopedTranslations
      description: >-
        The equivalent of `assets/i18n/<scope>/<lang>.json`. Keys are relative to the scope, as in
        the asset files: transloco prefixes them with the scope alias itself.
      responses:
        '200':
          description: The bundle, empty when nothing has been seeded for this scope and locale.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TranslationBundle'
        '403':
          $ref: '#/components/responses/Forbidden'

components:
  parameters:
    TranslationLocaleParam:
      name: locale
      in: path
      required: true
      description: BCP-47 language tag of the bundle to read.
      schema:
        $ref: '#/components/schemas/Locale'

    TranslationScopeParam:
      name: scope
      in: path
      required: true
      description: Transloco scope of the bundle to read.
      schema:
        $ref: '#/components/schemas/TranslationScope'

    OrgKeyParam:
      name: orgKey
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/OrganizationKey"
      description: >-
        Tenant discriminator; see shared-api.yaml OrgKeyParam for the full contract. Must be
        verified against the principal's `organization` claim — 403 on mismatch.
    EntityNameParam:
      name: entityName
      in: path
      required: true
      description: Unique within the organization; same value as base-entity-api.yaml's EntityDefinition.entityName.
      schema:
        type: string
    ObjectIdParam:
      name: objectId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    WhereParam:
      name: where
      in: query
      required: false
      schema:
        type: string
      description: >-
        RSQL filter expression. Over StateMachineDefinition fields (entityName, name) on
        /state-machines, and over DiagramDefinition's own scalars (entityName, updatedAt) on
        /diagrams — a diagram's nodes and edges are a serialized JSON blob and are not
        filterable. See shared-api.yaml WhereParam for the operator reference.
    OrderParam:
      name: order
      in: query
      required: false
      schema:
        type: string
      description: Format `property,(asc|desc)`; multiple criteria separated by comma.
    PageParam:
      name: page
      in: query
      required: false
      schema:
        type: integer
        default: 0
    SizeParam:
      name: size
      in: query
      required: false
      schema:
        type: integer
        default: 20

  responses:
    BadRequest:
      description: Validation error — the request payload or parameters are invalid.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: The authenticated principal may not act on this organization.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: The requested resource does not exist.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: The request conflicts with the current state of the resource.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"

  schemas:

    # ── Knowledge layer ────────────────────────────────────────

    GuardRef:
      type: object
      required: [ beanName ]
      description: >-
        Names a Spring bean implementing TransitionGuard, resolved by bean name — same named
        SPI convention as base-state's earlier TransitionGuard/TransitionAction sketch and
        base-entity's PayloadValidator seam. Existence of the bean is checked at definition
        save time, not just at transition time, so a typo surfaces immediately rather than
        the first time the transition is fired.
      properties:
        beanName:
          type: string
          example: sufficientBalanceGuard
        params:
          type: object
          nullable: true
          description: Static configuration passed to TransitionGuard.evaluate() alongside the runtime TransitionContext.
          additionalProperties: true

    ActionRef:
      type: object
      required: [ beanName ]
      description: Names a Spring bean implementing TransitionAction, resolved by bean name. Only run after every guard on the transition has passed.
      properties:
        beanName:
          type: string
          example: sendApprovalNotificationAction
        params:
          type: object
          nullable: true
          additionalProperties: true

    State:
      type: object
      required: [ key, name ]
      description: A single, flat, mutually exclusive value of the state attribute. No parallel or nested states in this version — see StateMachineDefinitionInput.
      properties:
        key:
          type: string
          description: Unique within the state machine; the literal value written to the state attribute.
          example: PendingApproval
        name:
          type: string
        description:
          type: string
          nullable: true
        isFinal:
          type: boolean
          default: false
          description: No transition may declare this state as its sourceStateKey — enforced at definition save time.
        isLocked:
          type: boolean
          default: false
          description: >-
            When true, only the state attribute itself may change on an object sitting in
            this state — every other attribute update via base-entity's
            updateEntityObject is rejected while the object is in this state. All-or-nothing
            for now; an attribute-level allow-list is a deliberate follow-up, the same way
            parallel/nested states are, not part of this version.
        metadata:
          type: object
          nullable: true
          description: UI hints only — e.g. color, icon — for rendering the state machine graph. Opaque to base-state-backend.
          additionalProperties: true

    Transition:
      type: object
      required: [ key, sourceStateKey, targetStateKey, triggerKey ]
      properties:
        key:
          type: string
          description: Unique within the state machine.
        name:
          type: string
          nullable: true
        sourceStateKey:
          type: string
          description: Must resolve to a declared State.key whose isFinal is not set.
        targetStateKey:
          type: string
          description: Must resolve to a declared State.key.
        triggerKey:
          type: string
          description: >-
            The verb callers invoke in TransitionRequest.triggerKey — e.g. "approve". Must
            be unique in combination with sourceStateKey; two transitions may share a
            triggerKey only if their sourceStateKey differs, and two transitions may share a
            sourceStateKey only if their triggerKey differs. Distinct transitions with the
            same triggerKey may target different states, which is how one button can mean
            different things depending on where the object currently sits.
          example: approve
        guards:
          type: array
          description: Evaluated in order, AND semantics, short-circuiting on the first that rejects. Empty means the transition is unconditional.
          items:
            $ref: "#/components/schemas/GuardRef"
        actions:
          type: array
          description: Executed in order, only once every guard has passed.
          items:
            $ref: "#/components/schemas/ActionRef"

    StateMachineDefinitionInput:
      type: object
      required: [ entityName, name, stateAttributeKey, initialStateKey, states ]
      properties:
        entityName:
          type: string
          description: >-
            The entity type this state machine governs — resolved within this organization.
            1:1: creating a second definition for the same entityName is a 409. The path
            segment on PUT/DELETE is the source of truth; this field is only meaningful on
            POST create, mirroring EntityDefinitionInput.entityName in base-entity-api.yaml.
        name:
          type: string
        description:
          type: string
          nullable: true
        stateAttributeKey:
          type: string
          description: >-
            The attrName, declared on entityName's current EntityDefinition, whose value
            holds the object's current state. Must be a TEXT- or ENUM-valued attribute —
            checked against that definition on every save, so a typo or a since-removed
            attribute is a 400 rather than a machine that fails the first time it is fired.
            Requiring the attribute to exist is also what restricts state machines to entity
            types base-entity-backend manages. base-state is the only writer of it — see the
            top-level description's "Ownership and the single entry point" note.
          example: state
        initialStateKey:
          type: string
          description: >-
            The State.key a newly-created EntityObject of this entityName starts in. Must
            resolve to a declared State.key. Written onto the object by base-state when it is
            created — see "Ownership and the single entry point" — so an object whose state
            attribute reads empty is one created before its machine existed, not a normal one.
        states:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/State"
        transitions:
          type: array
          items:
            $ref: "#/components/schemas/Transition"

    StateMachineDefinition:
      allOf:
        - $ref: "#/components/schemas/StateMachineDefinitionInput"
        - type: object
          properties:
            orgKey:
              $ref: "#/components/schemas/OrganizationKey"
            version:
              type: integer
              format: int64
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time

    PageOf_StateMachineDefinition:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/StateMachineDefinition"
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
        number:
          type: integer
        size:
          type: integer

    # ── Diagram layer ──────────────────────────────────────────

    Point:
      type: object
      required: [ x, y ]
      description: A point in diagram coordinates, matching ng-diagram's own `Point`.
      properties:
        x:
          type: number
          format: double
        y:
          type: number
          format: double

    NodeSize:
      type: object
      required: [ width, height ]
      description: >-
        Node dimensions in diagram coordinates, matching ng-diagram's own `Size`. Named NodeSize
        rather than Size because the generated models share a package with the Jakarta validation
        annotations the generator itself emits: a schema called `Size` shadows
        `jakarta.validation.constraints.Size` and breaks every `@Size`-annotated field in the
        package, including StateMachineDefinition's.
      properties:
        width:
          type: number
          format: double
        height:
          type: number
          format: double

    NodeLayout:
      type: object
      required: [ stateKey, position ]
      description: >-
        Where one State sits on the canvas. An array of keyed rows rather than a map keyed by
        stateKey, for the same reason `states` and `transitions` above are arrays: the key stays
        visible inside the row, which is what makes an exported layout readable.

        Carries only what the current, flat State model can express. Deliberately absent, and to
        be added when the feature that needs them lands rather than speculatively: `angle` (a
        state is never rotated), `zOrder` and `autoSize`, and any group membership — there are no
        nested or parallel states in this version, so a node is never inside another node.
      properties:
        stateKey:
          type: string
          description: >-
            The State.key this row positions. Not validated against the state machine's declared
            states — see saveDiagramDefinition's description for why a stale row is tolerated.
          example: PendingApproval
        position:
          $ref: "#/components/schemas/Point"
        size:
          # A bare $ref, not an allOf wrapper carrying `nullable`/`description`: a
          # single-element allOf is what degrades a generated type to Object, and OpenAPI 3.0
          # ignores siblings of a $ref anyway. Absent means the node is auto-sized by its
          # content, which is the default.
          $ref: "#/components/schemas/NodeSize"

    EdgeLayout:
      type: object
      required: [ transitionKey ]
      description: >-
        How one Transition is drawn. The port anchors matter as much as the waypoints do: an edge
        whose ports are not persisted is re-anchored on the next load, so the diagram reopens
        with visibly different geometry from the one the user arranged.
      properties:
        transitionKey:
          type: string
          description: The Transition.key this row routes. Not validated — see saveDiagramDefinition.
        points:
          type: array
          description: >-
            Intermediate waypoints, in order, in diagram coordinates — ng-diagram's `Edge.points`.
            Empty or absent means the edge is routed automatically between its two ports.
          items:
            $ref: "#/components/schemas/Point"
        sourcePort:
          type: string
          nullable: true
          description: The port on the source node the edge leaves from, e.g. `port-right`.
          example: port-right
        targetPort:
          type: string
          nullable: true
          description: The port on the target node the edge arrives at, e.g. `port-left`.
          example: port-left
        routing:
          type: string
          nullable: true
          description: >-
            The edge's routing mode as the user chose it — ng-diagram's `Edge.routing`. Opaque to
            base-state-backend, which stores and returns it without interpretation.
          example: orthogonal

    DiagramViewport:
      type: object
      required: [ x, y, scale ]
      description: >-
        Pan offset and zoom of the canvas. Persisted so that reopening a large machine returns to
        the part of it the user was working on, rather than to whatever an automatic fit chooses.
      properties:
        x:
          type: number
          format: double
        y:
          type: number
          format: double
        scale:
          type: number
          format: double
          example: 1.0

    DiagramDefinitionInput:
      type: object
      required: [ entityName ]
      description: >-
        The graphical layout of one entity type's state machine — purely presentational, and a
        resource of its own rather than fields on StateMachineDefinition. Two reasons: dragging a
        node must never risk a whole-document replace of the machine's topology, and the two are
        written by different gestures at very different rates.

        `nodes` and `edges` are both optional, because a machine that has been opened but never
        arranged legitimately has neither.
      properties:
        entityName:
          type: string
          description: >-
            The entity type whose state machine this lays out — 1:1 with it, and therefore also
            1:1 with the StateMachineDefinition of the same name. The path segment is the source
            of truth; this field is informational, mirroring StateMachineDefinitionInput.entityName.
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/NodeLayout"
        edges:
          type: array
          items:
            $ref: "#/components/schemas/EdgeLayout"
        viewport:
          # Bare $ref for the same reason NodeLayout.size is one.
          $ref: "#/components/schemas/DiagramViewport"

    DiagramDefinition:
      allOf:
        - $ref: "#/components/schemas/DiagramDefinitionInput"
        - type: object
          properties:
            orgKey:
              $ref: "#/components/schemas/OrganizationKey"
            version:
              type: integer
              format: int64
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time

    PageOf_DiagramDefinition:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/DiagramDefinition"
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
        number:
          type: integer
        size:
          type: integer

    # ── Operation layer ────────────────────────────────────────

    AvailableTransition:
      type: object
      required: [ transitionKey, triggerKey, targetStateKey, guardsSatisfied ]
      properties:
        transitionKey:
          type: string
        triggerKey:
          type: string
        targetStateKey:
          type: string
        guardsSatisfied:
          type: boolean
          description: Dry-run result — true only if every guard on this transition currently passes against the object's payload.
        blockedReason:
          type: string
          nullable: true
          description: Set when guardsSatisfied is false — the message from the first guard that rejected.

    EntityObjectStateView:
      type: object
      required: [ objectId, entityName, currentStateKey, isFinal, availableTransitions ]
      properties:
        objectId:
          type: string
          format: uuid
        entityName:
          type: string
        currentStateKey:
          type: string
        isFinal:
          type: boolean
          description: True when currentStateKey resolves to a State whose isFinal is true.
        enteredStateAt:
          type: string
          format: date-time
          nullable: true
          description: When the object last transitioned into currentStateKey. Absent if it has never transitioned since creation.
        availableTransitions:
          type: array
          items:
            $ref: "#/components/schemas/AvailableTransition"

    TransitionRequest:
      type: object
      required: [ triggerKey, version ]
      properties:
        triggerKey:
          type: string
          example: approve
        context:
          type: object
          nullable: true
          description: >-
            Caller-supplied data made available to guards and actions as
            TransitionContext.requestContext — e.g. an approval comment, or identifiers
            base-workflow-backend passes through from the activity that triggered this call.
          additionalProperties: true
        version:
          type: integer
          format: int64
          description: Optimistic lock against the EntityObject's current version — same contract as base-entity's updateEntityObject. A mismatch is a 409, not a business rejection.

    TransitionResult:
      type: object
      required: [ success, previousStateKey ]
      properties:
        success:
          type: boolean
        previousStateKey:
          type: string
        newStateKey:
          type: string
          nullable: true
          description: Absent when success is false — the object's state did not change.
        transitionKey:
          type: string
          nullable: true
          description: The Transition that matched (currentStateKey, triggerKey), even when a guard subsequently rejected it. Absent only when triggerKey matched no transition from the current state at all.
        executedActions:
          type: array
          items:
            type: string
          description: beanNames of the actions that ran, in execution order. Empty when success is false.
        rejectionReason:
          type: string
          nullable: true
          description: Populated when success is false — either "no transition for trigger '{triggerKey}' from state '{currentStateKey}'" or the rejecting guard's message.
        version:
          type: integer
          format: int64
          description: The EntityObject's version after this call — unchanged from the request when success is false.

    # Shared shapes — kept as local duplicates because the openapi-generator swagger-parser
    # cannot resolve cross-file $refs on Windows. schemaMappings in the pom.xml redirects the
    # generated classes to com.processpuzzle.shared.model, same mechanism base-rule-api.yaml
    # and base-entity-api.yaml already use for these three shapes.
    OrganizationKey:
      type: string
      description: URL-safe tenant slug; canonical definition in shared-api.yaml.
      pattern: '^[a-z0-9]+(-[a-z0-9]+)*$'
      minLength: 2
      maxLength: 63
      example: my-org

    ErrorResponse:
      type: object
      required: [ errorId, errorText ]
      properties:
        errorId:
          type: string
          example: state-machine.transition.ambiguous-trigger
        errorText:
          type: string
          example: Two transitions from state 'Draft' share the trigger 'submit'.

    ImportResult:
      type: object
      properties:
        created:
          type: integer
        updated:
          type: integer
        errors:
          type: array
          items:
            type: string

    Locale:
      type: string
      description: >-
        Repeat of shared-api.yaml's canonical Locale — see the note on WidgetInstance there for why
        this workspace repeats rather than $refs across files. A bare string schema generates no Java
        class, so the repetition needs no schemaMappings entry.
      pattern: '^[a-z]{2,3}(-[A-Z]{2})?$'
      minLength: 2
      maxLength: 6
      example: en

    TranslationScope:
      type: string
      description: >-
        Repeat of shared-api.yaml's canonical TranslationScope. Transloco scope a bundle loads under,
        and the directory segment its asset equivalent would have had.
      pattern: '^[a-z0-9]+(_[a-z0-9]+)*$'
      minLength: 1
      maxLength: 64
      example: base_app

    TranslationBundle:
      type: object
      description: >-
        Repeat of shared-api.yaml's canonical TranslationBundle, mapped onto the shared generated type.
        One transloco bundle: the whole message map of an (organization, scope, locale) triple, in the
        exact shape the frontend's asset file has. Keys are relative to the scope — transloco prefixes
        them with the scope alias itself.
      additionalProperties: true
      nullable: true
      example:
        module:
          name: Order administration
        route:
          orders: All orders
