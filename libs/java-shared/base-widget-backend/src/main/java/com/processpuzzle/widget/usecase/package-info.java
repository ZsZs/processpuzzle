/**
 * The use cases of the Base Widget feature, and the module's outward-facing surface.
 *
 * <p>Exposed as the {@code usecase} named interface. The nested {@code exception} package is not
 * propagated and stays internal, matching Base App.
 */
@NamedInterface("usecase")
package com.processpuzzle.widget.usecase;

import org.springframework.modulith.NamedInterface;
