/**
 * Runtime layer: interprets a {@code definition.domain.Workflow} to execute and monitor
 * process instances — task activation/completion/skip, artifact instance tracking, and the
 * ports/adapters that reach out to base-rule (preconditions/postconditions) and external tools.
 */
package com.processpuzzle.workflow.execution;
