/**
 * Design-time layer: authoring and storage of process definitions (roles, work products, tasks)
 * and tool definitions. Mirrors the {@code definition} / {@code instances} split in
 * base-entity-backend — this is the {@code definition} half, {@link com.processpuzzle.workflow.execution}
 * is the runtime half.
 */
package com.processpuzzle.workflow.definition;
