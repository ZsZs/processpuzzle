/**
 * Domain model of the ProcessPuzzle base entity.
 *
 * <p>Exposed as the {@code domain} named interface — Base Rule validates an entity change and Base
 * State projects an entity's current state, so both need to see the descriptors an entity is
 * described by.
 */
@NamedInterface("domain")
package com.processpuzzle.entity.domain;

import org.springframework.modulith.NamedInterface;
